# Convex Configuration
# Required: Your Convex deployment URL
# Get this from your Convex dashboard after running `npx convex dev`
NEXT_PUBLIC_CONVEX_URL=your-convex-url-here

# Site Configuration
# Optional: Your public site URL (used for QR codes, email links, etc.)
# If not set, will use VERCEL_URL in production or localhost in development
NEXT_PUBLIC_SITE_URL=https://your-site.vercel.app

# Base URL for email marketing
# Optional: Base URL for email unsubscribe links and tracking
# Defaults to http://localhost:3000 in development
NEXT_PUBLIC_BASE_URL=https://your-site.vercel.app

# Admin Authentication (Optional - can also be configured in Convex dashboard)
# Primary admin email for password resets
PRIMARY_ADMIN_EMAIL=your-admin@email.com

# Comma-separated list of allowed admin emails
ALLOWED_ADMIN_EMAILS=your-admin@email.com,another-admin@email.com

# Stripe Configuration (Optional - only needed for payment features)
# Stripe secret key for payment processing
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Stripe Price ID for storage subscriptions
STRIPE_STORAGE_PRICE_ID=price_your_storage_price_id

# Note: VERCEL_URL is automatically set by Vercel in production
# You don't need to set it manually

